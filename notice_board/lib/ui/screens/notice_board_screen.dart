import 'package:flutter/material.dart';

class NoticeBoardScreen extends StatelessWidget {

  static const routeName='/notices';

  @override
  Widget build(BuildContext context) {
    return Container(
     child: Text('this is notice board screen'),
    );
  }
}
